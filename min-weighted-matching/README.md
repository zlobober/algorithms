Compile in most optimized maner:
```
$ c++ main.cpp -o main -O2
```
Compile in most verbose self-checking debug manner:
```
$ c++ main.cpp -o main -g -DHEAVY_ASSERT_CONSISTENCY -DDEBUG -DEVERBOSE
```
